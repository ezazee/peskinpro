RewriteEngine on

# Redirect to public directory
RewriteCond %{HTTP_HOST} ^(www\.)?staging.peskinpro.id$ [NC]
RewriteCond %{REQUEST_URI} !^/public/
RewriteRule ^(.*)$ /public/$1 [L]

# Storage
RewriteRule ^storage/(.*)$ /storage/$1 [L]